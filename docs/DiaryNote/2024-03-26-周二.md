```c
int Partition(datalist &list, int low, int hight){
	int k = low;
	Element pivot = list.V[low];
	int i = low + 1;
	while(i <= high){
		if (list.V[i].getKey() < pivot.getKey() && ++k != i)
			Swap(list.V[k], list.V[i]);
		i ++;
	}
	Swap(list.V[k], list.V[low]);
	return k;
}
```